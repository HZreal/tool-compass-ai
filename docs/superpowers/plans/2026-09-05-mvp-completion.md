# MVP 完成与线上验收计划

基线：8991814；保留已部署的 Sites 项目和现有 Git worktree。用户已授权并行实施、验证及延续私有部署。既有需求与 technical-design 为依据。

| 任务 | 负责 | 依赖/共享接口 | 状态 |
| --- | --- | --- | --- |
| 结构化定价、检索边界、空库读取无副作用 | 主 agent | 0006 pricing_model；后台消费相同枚举 | 已完成 |
| 分类/场景/标签 CRUD 排序、投稿转草稿、精选排序 | admin agent | 0007 tags/tool_tags/converted_tool_id；主 agent 同步 schema | 已完成 |
| 国内产品官方资料核验 | curation agent | 返回来源/记录，主 agent 集成 | 已完成（待线上导入） |
| 严格类型、导航/可访问性、运维说明 | quality agent | 主 agent 提供备份 API；不改后台与 catalog 实现 | 已完成 |
| 管理员备份/恢复、一次性内容导入 | 主 agent | schema 就绪；所有操作鉴权、验证、批事务 | 已完成（待真实管理员导入） |
| 集成测试、独立审查、线上验收与部署记录 | 主 agent | 所有任务 | 部分完成（线上管理员验收待做） |

实施裁定：用户明确允许并行开发，因此采用独立文件所有权分工；仅主 agent 调用 Sites 或推送发布。迁移只改 schema，内容通过鉴权导入；去除公开 GET 写库。公开目录暂采用每次重新验证，保证编辑立即可见，缓存 5 分钟作为后续性能优化，不能用无法真正清理的缓存冒充失效机制。备份恢复先在独立本地数据库演练，不覆盖线上业务数据来做测试。

完成证据与剩余阻塞在本文件和 docs/project-status.md 同步记录。

2026-09-23 复核裁定：资料导入是唯一管理员的生产写操作，且 `DB.catalog_imports` 为空，不能把打包资料或本地测试当作已上线内容。自动化浏览器会话在读取登录态时超时；保持服务端精确鉴权，不以平台通行 token 或伪造身份头替代。完成导入后的目录检索、后台草稿/投稿/备份验收仍是严格 MVP 的最后闭环。
