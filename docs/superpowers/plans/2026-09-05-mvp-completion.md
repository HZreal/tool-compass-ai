# MVP 完成与线上验收计划

基线：8991814；保留已部署的 Sites 项目和现有 Git worktree。用户已授权并行实施、验证及延续私有部署。既有需求与 technical-design 为依据。

| 任务 | 负责 | 依赖/共享接口 | 状态 |
| --- | --- | --- | --- |
| 结构化定价、检索边界、空库读取无副作用 | 主 agent | 0006 pricing_model；后台消费相同枚举 | 进行中 |
| 分类/场景/标签 CRUD 排序、投稿转草稿、精选排序 | admin agent | 0007 tags/tool_tags/converted_tool_id；主 agent 同步 schema | 进行中 |
| 国内产品官方资料核验 | curation agent | 返回来源/记录，主 agent 集成 | 进行中 |
| 严格类型、导航/可访问性、运维说明 | quality agent | 主 agent 提供备份 API；不改后台与 catalog 实现 | 进行中 |
| 管理员备份/恢复、一次性内容导入 | 主 agent | schema 就绪；所有操作鉴权、验证、批事务 | 待做 |
| 集成测试、独立审查、线上验收与部署记录 | 主 agent | 所有任务 | 待做 |

实施裁定：用户明确允许并行开发，因此采用独立文件所有权分工；仅主 agent 调用 Sites 或推送发布。迁移只改 schema，内容通过鉴权导入；去除公开 GET 写库。公开目录暂采用每次重新验证，保证编辑立即可见，缓存 5 分钟作为后续性能优化，不能用无法真正清理的缓存冒充失效机制。备份恢复先在独立本地数据库演练，不覆盖线上业务数据来做测试。

完成证据与剩余阻塞在本文件和 docs/project-status.md 同步记录。
